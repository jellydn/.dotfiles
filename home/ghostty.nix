{
  config,
  pkgs,
  lib,
  ...
}:

{
  # ── Ghostty Terminal (macOS only) ───────────────────────────
  # Config file is symlinked from dotfiles repo via home/dotfiles-links.nix.
  # The Ghostty GUI app is installed via Homebrew casks (darwin.nix),
  # not via Nix packages.
}
